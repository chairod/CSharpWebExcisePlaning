<md-dialog flex="98" flex-gt-sm="35">
    <md-toolbar>
        <div class="md-toolbar-tools">
            <h2>ฟอร์มกำหนดข้อมูลสินทรัพย์</h2>
        </div>
    </md-toolbar>
    <md-dialog-content>
        <div class="md-dialog-content">
            <div class="form-row">
                <div class="form-group col-12">
                    <label class="required-field">ประเภทสินทรัพย์<fw-validate-error-output error-messages="$settings.formErrors.AssetTypeId.ErrorMessages" css-class="ml-1"></fw-validate-error-output></label>
                    <select class="form-control" ng-model="$settings.formData.AssetTypeId" ng-disabled="$settings.isLoading" fw-select2>
                        <option value="empty">--- ประเภทสินทรัพย์ ---</option>
                        @foreach (var assetItem in ViewBag.AssetTypes)
                        {
                            <option value="@assetItem.ASSET_TYPE_ID">@assetItem.ASSET_TYPE_NAME</option>
                        }
                    </select>
                </div>
                <div class="form-group col-12">
                    <label class="required-field">ชื่อสินทรัพย์<fw-validate-error-output error-messages="$settings.formErrors.AssetName.ErrorMessages" css-class="ml-1"></fw-validate-error-output></label>
                    <input type="text" class="form-control" ng-model="$settings.formData.AssetName" 
                           ng-disabled="$settings.isLoading||$settings.formData.AssetOtherFlag==1" maxlength="100" />
                </div>
                <div class="form-group col-12">
                    <label class="d-none">&nbsp;</label>
                    <md-checkbox ng-model="$settings.formData.AssetOtherFlag" class="mb-0" 
                                 ng-click="assetOtherFlagClick()"
                                 ng-true-value="1" ng-false-value="0" ng-disabled="$settings.isLoading">สินทรัพย์นี้เป็นสินทรัพย์อื่นๆใช่หรือไม่</md-checkbox>
                    <span class="text-danger d-block f-12">ถ้าเลือกเป็นสินทรัพย์อื่นๆ ระบบจะใช้เงื่อนไขนี้แสดงช่องให้ระบุข้อมูลเพิ่มเติม</span>
                </div>
            </div>
        </div>
    </md-dialog-content>
    <md-dialog-actions layout="row">
        <span flex></span>
        <fw-execute-button text="บันทึก" css-class="btn btn-primary btn-sm mr-2"
                           ng-disabled="$settings.isLoading"
                           css-icon-class="ti-save"
                           ng-click="save($event)"></fw-execute-button>
        <fw-execute-button text="ปิดหน้าต่าง" css-class="btn btn-danger btn-sm"
                           ng-disabled="$settings.isLoading"
                           css-icon-class="ion-close-round"
                           ng-click="close()"></fw-execute-button>
    </md-dialog-actions>
</md-dialog>